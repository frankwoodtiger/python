import Node

class BinaryTree:
	"""Binary tree class"""
	def __init__(self, rootLoad)
		self.root = Node(rootLoad)
		
	def insert(self, node):
		tempNode = root
		while tempNode is not None:
			if tempNode.getLoad
			